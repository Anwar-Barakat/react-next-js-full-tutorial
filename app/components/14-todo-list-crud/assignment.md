# Assignment: Todo List CRUD

In this exercise, you will implement a basic Todo List application with Create, Read, Update, and Delete (CRUD) functionalities.

## Step 1: Implement the TodoList Component

The `TodoList` component is already provided. You will move it to the correct lecture folder and ensure it functions correctly.

## Step 2: Enhance TodoList Component (Optional - for future steps)

(This step is a placeholder for future enhancements like styling, editing, etc.)

## Step 3: Render the Component in `app/page.tsx`

In your `app/page.tsx` file, import the `TodoList` component from the new lecture folder.
Inside the `Home` component's return statement, render the `TodoList` component.
